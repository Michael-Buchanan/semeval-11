This project explores two solutions to SemEval task 11. 

1) A neurosymbolic logical solver that uses an LLM to convert raw data into a JSON format that a symbolic logical solver can process.
2) Fine tuning GPT-5 on the training data.
